// Package linux supplies the Linux element and geometry layer.
//
// Tree walking on Linux is AT-SPI's job and lives in accessibility/atspi; what
// is here is the input injection and window geometry that the AT-SPI client
// delegates to, plus the Element and TreeNode types the client shell in the
// parent package is written against.
package linux
