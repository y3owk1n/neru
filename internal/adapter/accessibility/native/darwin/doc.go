// Package darwin reads the accessibility tree through AXUIElement.
//
// The whole directory is darwin-only, so the directory carries the platform and
// the filenames do not repeat it. It supplies the Element, ElementInfo,
// TreeNode and TreeOptions types the client shell in the parent package is
// written against.
package darwin
