// Package windows reads the accessibility tree through UI Automation over COM.
//
// The whole directory is windows-only, so the directory carries the platform
// and the filenames do not repeat it. It supplies the Element, ElementInfo,
// TreeNode and TreeOptions types the client shell in the parent package is
// written against.
package windows
