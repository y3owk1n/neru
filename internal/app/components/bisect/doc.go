// Package bisect holds the state one bisect session keeps: the selection it
// has made and the per-activation flags a binding asked for. It is mode
// state, not drawing, kept per mode the way every other session context is.
package bisect
