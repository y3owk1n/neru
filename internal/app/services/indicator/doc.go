// Package indicator carries the half of a cursor-following indicator's life
// that is the same for all of them: the cursor read every position update
// starts from, and visibility.
package indicator
