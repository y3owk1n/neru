// Package recursivegrid renders the recursive-grid overlay: the nested cells
// and their key labels that let a selection be refined one level at a time.
//
// It owns style and drawing only; the subdivision maths and selection state
// live in internal/core/domain/recursivegrid.
package recursivegrid
